<%= form_tag status_path, method: :patch, class: 'status-button'  do %>
  <%= hidden_field_tag 'game_id', game_id %>
  <%= hidden_field_tag 'invite_id', invite_id %>
  <%= hidden_field_tag 'status', status %>
  <%= submit_tag button_name, class: 'btn btn-default' %>
<% end %>
